"use client";

import React from "react";

/**
 * Placeholder component — will be replaced later
 * when Post features (blog, journaling, etc.) are added.
 */
export function LatestPost() {
  return (
    <div className="w-full max-w-xs rounded-2xl bg-white/10 p-4 text-center text-white shadow-md backdrop-blur">
      <h2 className="text-lg font-semibold mb-2">Your Posts</h2>
      <p className="text-sm text-gray-300">
        No posts yet — coming soon.
      </p>
    </div>
  );
}
